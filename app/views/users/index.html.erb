<% if search=params[:search]  %>
<% title="Search results for #{search}" %>
<% else %>
<% title="All users" %>
<% end %>
<% provide(:title,title) %>
<%= content_tag(:h1, "#{title}") %>
<%= will_paginate %>

<ul class="users">
  <%= render @users %>
</ul>
